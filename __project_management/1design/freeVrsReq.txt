database requierments(free version):
    tables:
        - admin logins(adminId, username, password)
        - accounts(mallId, shopId, versionId)
        - verion(versionId, name, desc, price, time )
        - mall(Mallid, name, ...)
            - shopLogin(shopId, username, password,)
            - shops(shopId, mallId, name, contactInfo)
                - loacation(locId, shopId, lat, long, shopId)
                - products(prodId, shopId, name, desc, displayPrice, actualPrice, stockTag, categoryId, ...)
                    - ProdCategory(ProdCatId, name, keywordId)
                        - ProdCatKeyword(ProdKeywordId, categoryId, name)
                - services(servId, shopId, name, desc, displayPrice, actualPrice, categoryId, ... )
                    - servCategory(ServCatId, name, keywordId)
                        - servCatKeyword(ServKeywordId, categoryId, name)
            - Transactions(transId, prodTransHeadId, servTransHeadId, datetime)
            - ProdTransHeader(transHeaderId, shopId, custId, totalPrice ... )
                - ProdTransDetail(transDetailId, transHeaderId, productId, ProdPrice quantity )
            - ServTransHeader(transHeaderId, shopId, custId, totalPrice ... )
                - ServTransDetail(transDetailId, transHeaderId, productId, ProdPrice quantity )
            - socialMediaTypes(socMedId,shopId name, location)
            - customers(cusId, mallId, email, name, bankDetailsId, locationId )
                - location(locationId, name, city, postalCode, lat, long )
                - bankingDetails(accountName, accNum, accBank, accType)
            - customerLogin(cusId, username, password)
            - registeringCustomers(regId, email, registerToken, dateTime)
            - CusFavProd(cfpId, cusId, ProdCatId)
            - CusFavServ(cfsId, cusId, ServCatId)

